# =============================================================================
# K8s 编排文件 — riverli-blog-identity (身份认证服务)
# =============================================================================
# 说明：身份认证服务负责用户注册/登录/JWT 签发，集群内 RPC 调用，无需对外暴露。
#       ArgoCD / Jenkins 会自动将 :latest 替换为具体版本号（如 :v42）
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: riverli-identity
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: riverli-identity
  template:
    metadata:
      labels:
        app: riverli-identity
    spec:
      # 🔐 GitHub Container Registry 认证凭据
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: blog-identity
          # 🌟 Jenkins 自动替换版本号
          image: ghcr.io/strive9930/riverli-blog-identity:v7
          ports:
            - containerPort: 8080
          env:
            - name: ConsulConfig__Address
              value: "http://consul-server:8500"

---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: default
spec:
  # 默认 ClusterIP 类型，仅集群内部可访问
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: riverli-identity
